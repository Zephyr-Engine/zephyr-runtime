const Uuid = @import("uuid.zig").Uuid;

pub const ComponentTypeId = Uuid;
pub const SceneEntityId = Uuid;
pub const ProjectId = Uuid;
pub const SchemaId = Uuid;
pub const AssetId = Uuid;
pub const SceneId = Uuid;
